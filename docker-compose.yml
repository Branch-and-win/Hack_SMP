version: '3'
services:
#  model:
#    build: ./src/smp_scenario
#    volumes:
#      - ./:/app
#    ports:
#     - "5001:5001"
  dash:
    build: ./src/smp_dash
    volumes:
      - ./:/app
    ports:
      - "5000:5000"